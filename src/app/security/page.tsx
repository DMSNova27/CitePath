import Link from "next/link";

export default function SecurityPage() {
  return <main className="page-shell"><nav className="nav"><Link href="/" className="brand">CitePath</Link><Link href="/methodology" className="nav-link">Methodology</Link></nav><article style={{maxWidth:900,margin:"0 auto",padding:"56px 24px 100px"}}><p className="eyebrow">Security</p><h1 style={{fontSize:"clamp(42px,7vw,70px)",letterSpacing:"-.06em",margin:0}}>Untrusted URLs stay untrusted.</h1><p className="lead">The scanner is designed around public-web analysis, strict input validation and defensive network access.</p><section style={{marginTop:48,lineHeight:1.7,color:"#41444b"}}><h2>Network protections</h2><p>Submitted URLs are validated before retrieval. The analyzer rejects local/private destinations, credentials in URLs, unsupported protocols, excessive redirects and oversized responses, and uses timeouts and bounded reads.</p><h2>Content handling</h2><p>Website content is treated as untrusted input. The product does not bypass authentication, CAPTCHAs, paywalls or private content.</p><h2>Production review</h2><p>Security-sensitive crawler behavior requires further testing, dependency review, rate limiting at the deployment layer and independent security review before production launch. This page does not claim that the application is bug-free or secure against every attack.</p></section></article></main>;
}
